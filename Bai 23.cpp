#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
int a,b,cv,dt;
cin >> a >> b;
cv = (a+b) * 2;
dt = a * b;
cout << cv <<" "<< dt;
    return 0;
}

