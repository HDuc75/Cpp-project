#include <bits/stdc++.h>
#include <cmath>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
	double a,b;
	cin >> a;
	b = sqrt(a);
	cout << fixed << setprecision(2) << b;
    return 0;
}

