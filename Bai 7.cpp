#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
	double a,b;
	cin >> a;
	b = 1/a;
	cout << fixed << setprecision(5) << b;
    return 0;
}

